TODO:

ADD interactive shell to ask if user the below
  What file types do you want to collect (audio, .adv, .amxd, etc)
  Would you like to save UserLibrary files?
  Any folders we should look at in case of missing files? (only ask once missing files are found)

CLI Config setup
    > Config struct {
        ask_mode: per_session, per_audio, none
        cmd: Mode,
        copy_mode: all, recorded, plugins, imported
      }
    add interactive mode
    first run check -> then if accepted collect
  
! nodesByName should have a ref to the map to deduplicate before ending up in the main func
  will cut allocations in half if not more

ask for user input before each file is scanned for unique types of scan
  also allow global scan type for all files

allow for options to only collect certain File types (AbletonPreset, AbletonPlugin, RecordedFile, etc)
  ensure file is not already collected before copying
  ^^ implemented (but maybe check file size before asserting true)

create c ABI funcs to wrap the api

BONUS: avoid name collisions (maybe copy file structure into the collected folder)
BONUS: modify existing XML to use resolved file path locations

EXTRA BONUS: resolve duplicates or crop files to only used length to reduce disk space
  so unnecessary
